function searchbarSearch(element) {
    if (element.keyCode === 13) {
        search(element);
    }
}

function search(element) {
    // TODO find page with the closest title to element.value and go there
}
